import React from 'react';import Logo from "../../Images/logo.png";import { loginAction } from "../../Store/Actions/PublicActions";import { useDispatch } from 'react-redux';function Login() {  let dispatch = useDispatch();  let loginFormData = {    username: null,    password: null  }  const login = e => {    e.preventDefault();    dispatch(loginAction(loginFormData))  }  return (    <div className="Login m0a">      <div className="Login__box flex-column justify-center box-shadow">        <img className="Login__logo" src={ Logo } alt="Website logo"/>        <form className="flex-column p-2" onSubmit={ (e) => login(e) }>          <input            type="text"            name="username"            required            placeholder="Enter your Tesonet username"            className="Login__input mt b-sizing-border p fs-medium"            onChange={(e) => loginFormData.username = e.target.value }          />          <input            type="password"            name="password"            required            placeholder="Enter your Tesonet password"            className="Login__input mt b-sizing-border p fs-medium"            onChange={(e) => loginFormData.password = e.target.value }          />          <button className="Login__button mt-2 fs-large fw-600" type="submit" >Login</button>        </form>      </div>    </div>  );}export default Login;